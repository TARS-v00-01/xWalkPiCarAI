/******************************************************************************
 * @file        xHal_Rpi5CarXIwMessageCfm.proto
 * @brief       Defines public Protobuf confirmation messages for xWalk interfaces.
 *
 * @details
 * Defines one successful confirmation counterpart for every request message in
 * xHal_Rpi5CarXIwMessageReq.proto.
 *
 * @project     xWalk Firmware
 * @module      xWalk-rpi5-iw
 *
 * @author      Joxy John
 * @date        2026-08-07
 * @version     1.0.0
 *
 * @copyright
 * Copyright (c) 2026 Joxy John.
 * All rights reserved.
 *
 * @note
 * Developed using MISRA C++ coding guidelines.
 ******************************************************************************/
syntax = "proto3";
package xwalk.iw.v1;

/******************************************************************************
 * Imports
 ******************************************************************************/

import "xHal_Rpi5CarXIwMessageCmn.proto";
import "xHal_Rpi5CarXIwMessageReq.proto";
import "xHal_Rpi5CarXIwSignal.proto";

/******************************************************************************
 * Confirmation message declarations
 ******************************************************************************/

/**
 * @brief Confirms successful handling of XWalkI2cRequestPayload.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkI2cCfmPayload
{
    option (cxx_signal) = CXX_XWALK_I2C_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned by a successful read operation in bus order.
    bool responding = 2; //@@ Indicates whether the addressed device responded to a probe.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkControllerCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkControllerCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CTRL_CMD_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkNoArgumentRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkNoArgumentCfm
{
    option (cxx_signal) = CXX_XWALK_NO_ARG_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkLifecycleRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkLifecycleCfm
{
    option (cxx_signal) = CXX_XWALK_LIFECYCLE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkMoveRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkMoveCfm
{
    option (cxx_signal) = CXX_XWALK_MOVE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkTurnRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkTurnCfm
{
    option (cxx_signal) = CXX_XWALK_TURN_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkCameraRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkCameraCfm
{
    option (cxx_signal) = CXX_XWALK_CAMERA_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSensorRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSensorCfm
{
    option (cxx_signal) = CXX_XWALK_SENSOR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSelfDriveRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSelfDriveCfm
{
    option (cxx_signal) = CXX_XWALK_SELF_DRIVE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSpiRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSpiCfm
{
    option (cxx_signal) = CXX_XWALK_SPI_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkGptCarRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkGptCarCfm
{
    option (cxx_signal) = CXX_XWALK_GPT_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkCalibrationRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkCalibrationCfm
{
    option (cxx_signal) = CXX_XWALK_CALIBRATION_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSoundRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSoundCfm
{
    option (cxx_signal) = CXX_XWALK_SOUND_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkUnknownCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkUnknownCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_UNKNOWN_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkHelpCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkHelpCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_HELP_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSpiCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSpiCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SPI_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkDoctorCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkDoctorCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_DOCTOR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkServoZeroingCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkServoZeroingCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SERVO_ZEROING_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkComputerVisionCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkComputerVisionCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_COMPUTER_VISION_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkRecordVideoCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkRecordVideoCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_RECORD_VIDEO_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSoundBackgroundMusicCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSoundBackgroundMusicCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SOUND_BACKGROUND_MUSIC_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkTextVisionTalkCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkTextVisionTalkCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_TEXT_VISION_TALK_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkOnlineLlmTestCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkOnlineLlmTestCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_ONLINE_LLM_TEST_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkMoveCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkMoveCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_MOVE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkKeyboardControlCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkKeyboardControlCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_KEYBOARD_CONTROL_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkAvoidObstaclesCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkAvoidObstaclesCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_AVOID_OBSTACLES_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkCliffDetectionCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkCliffDetectionCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_CLIFF_DETECTION_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkStareAtYouCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkStareAtYouCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_STARE_AT_YOU_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkBullFightCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkBullFightCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_BULL_FIGHT_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkTreasureHuntCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkTreasureHuntCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_TREASURE_HUNT_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVideoCarCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkVideoCarCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VIDEO_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkAppControlCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkAppControlCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_APP_CONTROL_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkTurnCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkTurnCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_TURN_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkCameraCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkCameraCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_CAMERA_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSensorCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSensorCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SENSOR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkLineTrackCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkLineTrackCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_LINE_TRACK_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSelfDriveCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSelfDriveCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SELF_DRIVE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkSoundCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkSoundCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_SOUND_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVoiceChatCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkVoiceChatCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_CHAT_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVoiceActiveCarCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkVoiceActiveCarCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_ACTIVE_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkGptCarCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkGptCarCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_GPT_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVoiceControlledCarCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkVoiceControlledCarCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_CONTROLLED_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVoicePromptCarCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkVoicePromptCarCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_PROMPT_CAR_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkStorytellingRobotCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkStorytellingRobotCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_STORYTELLING_ROBOT_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkCalibrateCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the corresponding request.
 */
message XWalkCalibrateCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_CALIBRATE_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    bool flag = 3; //@@ Indicates whether the request completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}

/**
 * @brief Confirms successful handling of XWalkVideoStreamCommandRequest.
 *
 * @details
 * Reports the standardized confirmation state for the video-stream request.
 */
message XWalkVideoStreamCommandCfm
{
    option (cxx_signal) = CXX_XWALK_CMD_VIDEO_STREAM_CFM;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the response in transport order.
    bool responding = 2; //@@ Indicates whether the streaming service responded.
    bool flag = 3; //@@ Indicates whether streaming completed successfully.
    string message = 4; //@@ Human-readable confirmation message.
}
