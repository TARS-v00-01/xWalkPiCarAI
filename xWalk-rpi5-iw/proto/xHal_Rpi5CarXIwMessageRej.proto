/******************************************************************************
 * @file        xHal_Rpi5CarXIwMessageRej.proto
 * @brief       Defines public Protobuf rejection messages for xWalk interfaces.
 *
 * @details
 * Defines the trace diagnostic transport message and one rejection counterpart
 * for every request message in xHal_Rpi5CarXIwMessageReq.proto.
 *
 * @project     xWalk Firmware
 * @module      xWalk-rpi5-iw
 *
 * @author      Joxy John
 * @date        2026-08-07
 * @version     1.0.0
 *
 * @copyright
 * Copyright (c) 2026 Joxy John.
 * All rights reserved.
 *
 * @note
 * Developed using MISRA C++ coding guidelines.
 ******************************************************************************/
syntax = "proto3";
package xwalk.iw.v1;

/******************************************************************************
 * Imports
 ******************************************************************************/

import "xHal_Rpi5CarXIwMessageCmn.proto";
import "xHal_Rpi5CarXIwMessageReq.proto";
import "xHal_Rpi5CarXIwSignal.proto";

/******************************************************************************
 * Rejection message declarations
 ******************************************************************************/

/**
 * @brief Carries an error or warning emitted through the trace callback.
 *
 * @details
 * Carries the stable selector number, callback severity, and fully formatted
 * diagnostic message. This transport-only message has no request signal binding.
 */
message XWalkTraceRej
{
    XWalkTraceSeverity severity = 1; //@@ Error or warning severity supplied to the trace callback.
    string message = 2; //@@ Fully formatted diagnostic text supplied to the trace callback.
    XWalkErrorSignalNumber error_signal = 3; //@@ Stable number identifying the public trace selector.
    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.
}

/**
 * @brief Rejects XWalkI2cRequestPayload with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkI2cRejPayload
{
    option (cxx_signal) = CXX_XWALK_I2C_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkControllerCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkControllerCommandRej
{
    option (cxx_signal) = CXX_XWALK_CTRL_CMD_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkNoArgumentRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkNoArgumentRej
{
    option (cxx_signal) = CXX_XWALK_NO_ARG_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkLifecycleRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkLifecycleRej
{
    option (cxx_signal) = CXX_XWALK_LIFECYCLE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkMoveRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkMoveRej
{
    option (cxx_signal) = CXX_XWALK_MOVE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkTurnRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkTurnRej
{
    option (cxx_signal) = CXX_XWALK_TURN_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkCameraRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkCameraRej
{
    option (cxx_signal) = CXX_XWALK_CAMERA_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSensorRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSensorRej
{
    option (cxx_signal) = CXX_XWALK_SENSOR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSelfDriveRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSelfDriveRej
{
    option (cxx_signal) = CXX_XWALK_SELF_DRIVE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSpiRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSpiRej
{
    option (cxx_signal) = CXX_XWALK_SPI_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkGptCarRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkGptCarRej
{
    option (cxx_signal) = CXX_XWALK_GPT_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkCalibrationRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkCalibrationRej
{
    option (cxx_signal) = CXX_XWALK_CALIBRATION_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSoundRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSoundRej
{
    option (cxx_signal) = CXX_XWALK_SOUND_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkUnknownCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkUnknownCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_UNKNOWN_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkHelpCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkHelpCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_HELP_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSpiCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSpiCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SPI_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkDoctorCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkDoctorCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_DOCTOR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkServoZeroingCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkServoZeroingCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SERVO_ZEROING_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkComputerVisionCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkComputerVisionCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_COMPUTER_VISION_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkRecordVideoCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkRecordVideoCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_RECORD_VIDEO_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSoundBackgroundMusicCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSoundBackgroundMusicCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SOUND_BACKGROUND_MUSIC_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkTextVisionTalkCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkTextVisionTalkCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_TEXT_VISION_TALK_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkOnlineLlmTestCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkOnlineLlmTestCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_ONLINE_LLM_TEST_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkMoveCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkMoveCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_MOVE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkKeyboardControlCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkKeyboardControlCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_KEYBOARD_CONTROL_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkAvoidObstaclesCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkAvoidObstaclesCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_AVOID_OBSTACLES_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkCliffDetectionCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkCliffDetectionCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_CLIFF_DETECTION_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkStareAtYouCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkStareAtYouCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_STARE_AT_YOU_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkBullFightCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkBullFightCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_BULL_FIGHT_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkTreasureHuntCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkTreasureHuntCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_TREASURE_HUNT_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVideoCarCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkVideoCarCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VIDEO_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkAppControlCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkAppControlCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_APP_CONTROL_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkTurnCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkTurnCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_TURN_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkCameraCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkCameraCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_CAMERA_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSensorCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSensorCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SENSOR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkLineTrackCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkLineTrackCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_LINE_TRACK_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSelfDriveCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSelfDriveCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SELF_DRIVE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkSoundCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkSoundCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_SOUND_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVoiceChatCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkVoiceChatCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_CHAT_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVoiceActiveCarCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkVoiceActiveCarCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_ACTIVE_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkGptCarCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkGptCarCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_GPT_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVoiceControlledCarCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkVoiceControlledCarCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_CONTROLLED_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVoicePromptCarCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkVoicePromptCarCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VOICE_PROMPT_CAR_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkStorytellingRobotCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkStorytellingRobotCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_STORYTELLING_ROBOT_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkCalibrateCommandRequest with a reason and diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the corresponding request.
 */
message XWalkCalibrateCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_CALIBRATE_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the addressed service or device responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}

/**
 * @brief Rejects XWalkVideoStreamCommandRequest with diagnostic detail.
 *
 * @details
 * Reports the standardized rejection state for the video-stream request.
 */
message XWalkVideoStreamCommandRej
{
    option (cxx_signal) = CXX_XWALK_CMD_VIDEO_STREAM_REJ;

    XClientAddress client_address = 15; //@@ Address and routing identity of the client associated with the message.

    bytes data = 1; //@@ Bytes returned with the rejection in transport order.
    bool responding = 2; //@@ Indicates whether the streaming service responded.
    uint32 reason = 3; //@@ Stable numeric reason identifying why the request was rejected.
    string detail = 4; //@@ Human-readable diagnostic detail describing the rejection.
    XWalkErrorSignalNumber error_signal = 5; //@@ Stable trace error selector identifying the rejection cause.
}
