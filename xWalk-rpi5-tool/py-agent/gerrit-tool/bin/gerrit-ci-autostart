#!/bin/sh
set -eu
# shellcheck source=/dev/null
. "$HOME/gerrit-env.sh"

cleanup()
{
    "$HOME/bin/gerrit-ci-control" stop || true
}

trap cleanup EXIT
trap 'exit 0' HUP INT TERM

"$HOME/bin/gerrit-ci-control" start
while [ -r "$GERRIT_SITE/logs/gerrit.pid" ]; do
    sleep 5
done
