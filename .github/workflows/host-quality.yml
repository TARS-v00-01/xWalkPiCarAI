name: xWalk Host Quality

on:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: xwalk-host-quality-${{ github.sha }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  preparation:
    name: xWalk Preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Test Gerrit push routing
        run: python3 .github/tests/test_git_environment.py
      - name: Test Gerrit checkout contract
        run: python3 .github/actions/checkout-private-submodules/test_checkout.py
      - name: Validate xWalk host dependencies
        run: xWalk-rpi5-tool/shell-agent/quality-tool/check-host-quality-dependencies.sh
      - name: Validate shell scripts and CI metadata
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh preparation
      - name: Validate C++ formatting
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh styler
      - name: Package exact submitted sources for secret-free jobs
        run: |
          tar --create --file "$RUNNER_TEMP/xwalk-submitted-sources.tar" \
            --exclude='*/.git' \
            devloper-note \
            xWalk-rpi5-hw/xWalkDriver \
            xWalk-rpi5-hw/xWalkAudioResources \
            xWalk-rpi5-hw/xWalkController \
            xWalk-rpi5-hw/xWalkHal \
            xWalk-rpi5-hw/xWalkLibrary \
            xWalk-rpi5-trace \
            xWalk-rpi5-iw \
            xWalk-rpi5-node \
            xWalk-rpi5-tool
      - uses: actions/upload-artifact@v6
        with:
          name: xwalk-submitted-sources-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/xwalk-submitted-sources.tar
          compression-level: 0
          retention-days: 1

  xwalk-agent:
    name: xWalkDriver
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build xWalkDriver
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-agent-build
      - name: Run xWalkDriver aggregate tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-agent-aggregate
      - name: Run xWalkDriver functional-group tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-agent-groups
      - name: Run xWalkDriver configuration and communication tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-agent-functional

  xwalk-controller:
    name: xWalkController
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure xWalkController settings
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-controller-build
      - name: Validate retained Controller configuration
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-controller-tests

  xwalk-hal:
    name: xWalkHal
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build xWalkHal
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-hal-build
      - name: Run HAL unit and host-safe sequence tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-hal-unit
      - name: Run HAL interface, device, sensor, and layer tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-hal-groups
      - name: Run Robot HAT and lifecycle simulations
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-hal-simulation
      - name: Run the device-free Robot HAT soak test
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-hal-soak

  xwalk-iw:
    name: xWalk-rpi5-iw
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build xWalk-rpi5-iw
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-iw-build
      - name: Run schema, serialization, gRPC, signal, and transport tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-iw-tests

  xwalk-library:
    name: xWalkLibrary
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build xWalkLibrary consumers
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-library-build
      - name: Run shared-library, configuration, utility, licence, and architecture tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-library-tests

  xwalk-trace:
    name: xWalk-rpi5-trace
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Build and run Trace formatting, routing, macro, and selector tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-trace-tests

  xwalk-vision:
    name: xWalk Vision
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build recorded vision scenarios
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-vision-build
      - name: Validate recorded media, OpenCV scenarios, safety, and assets
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-vision-tests

  xwalk-streaming:
    name: xWalk Streaming
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Configure and build xWalk Streaming
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-streaming-build
      - name: Run loopback HTTP, MJPEG, lifecycle, timeout, and backpressure tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh xwalk-streaming-tests

  xwalk-quality:
    name: xWalk Quality
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Run GCC Debug build and tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh build-and-test gcc Debug
      - name: Run GCC Release build and tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh build-and-test gcc Release
      - name: Run Clang Debug build and tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh build-and-test clang Debug
      - name: Run Clang Release build and tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh build-and-test clang Release
      - name: Run AddressSanitizer and UndefinedBehaviorSanitizer
        run: timeout --signal=TERM 20m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh asan-ubsan
      - name: Run LeakSanitizer
        run: timeout --signal=TERM 20m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh leak-sanitizer
      - name: Run ThreadSanitizer
        run: timeout --signal=TERM 10m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh thread-sanitizer
      - name: Run Clang-Tidy and Cppcheck
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh static-analysis
      - name: Run Clang Static Analyzer
        run: timeout --signal=TERM 30m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh clang-static-analyzer
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: clang-static-analyzer-report
          path: build-host/clang-analyzer/reports
          if-no-files-found: ignore
      - name: Run host stress tests
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh stress-tests
      - name: Run bounded fuzz smoke tests
        run: timeout --signal=TERM 10m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh fuzz-smoke
      - name: Run Valgrind analysis
        run: timeout --signal=TERM 30m xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh valgrind
      - name: Generate GCC and Clang coverage
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh coverage
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: xwalk-coverage-reports
          path: |
            build-host/coverage/coverage.html
            build-host/coverage/coverage.*.html
            build-host/coverage/coverage.xml
            build-host/coverage/coverage.json
            build-host/coverage-clang/coverage-summary.txt
            build-host/coverage-clang/coverage.json
            build-host/coverage-clang/html
          if-no-files-found: ignore

  xwalk-deployment:
    name: xWalk Deployment
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - name: Validate deployment and provisioning scripts
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh deployment-scripts
      - name: Validate ARM64 dependencies and configured cross-build
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh arm64
      - name: Build and validate the staged installation
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh staged-install
      - uses: actions/upload-artifact@v6
        with:
          name: staged-install-checksums
          path: build-host/deploy.sha256

  developer-note:
    name: Developer Documentation
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    steps:
      - uses: actions/checkout@v6
      - name: Check out exact submitted component commits
        uses: ./.github/actions/checkout-private-submodules
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Build and verify the searchable developer-note wiki
        run: xWalk-rpi5-tool/shell-agent/gerrit-tool/run-host-ci-job.sh developer-note-wiki
      - uses: actions/upload-artifact@v6
        with:
          name: xwalk-developer-note-wiki
          path: build-devloper-note-wiki/github-site

  codescene-code-health:
    name: MyPiCarX / Code Health
    needs: preparation
    runs-on: [self-hosted, linux, x64, xwalk-ci]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Download exact submitted component sources
        uses: actions/download-artifact@v6
        with:
          name: xwalk-submitted-sources-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}
      - name: Restore exact submitted component sources
        run: tar --extract --file "$RUNNER_TEMP/xwalk-submitted-sources.tar" --directory "$GITHUB_WORKSPACE"
      - name: Validate MyPiCarX CodeScene component configuration
        run: xWalk-rpi5-tool/py-agent/dev-tool/xWalkCodeHealth validate-config
      - name: Analyse changed code with the licensed CodeScene CLI when available
        env:
          GITHUB_BASE_SHA: ${{ github.event.before || github.sha }}
          GITHUB_HEAD_SHA: ${{ github.sha }}
          XWALK_CODESCENE_BASE_REVISION: ${{ github.event.before || github.sha }}
          XWALK_CODESCENE_REVISION: ${{ github.sha }}
          XWALK_CODESCENE_STRICT: ${{ vars.XWALK_CODESCENE_STRICT || 'false' }}
        run: xWalk-rpi5-tool/py-agent/dev-tool/xWalkCodeHealth analyze
      - uses: actions/upload-artifact@v6
        if: always()
        with:
          name: mypicarx-code-health-${{ github.run_id }}-${{ github.run_attempt }}
          path: build-host/codescene
          if-no-files-found: warn

  host-quality-gate:
    name: xWalk Host Quality Gate
    runs-on: ubuntu-24.04
    if: always()
    needs:
      - xwalk-agent
      - xwalk-controller
      - xwalk-hal
      - xwalk-iw
      - xwalk-library
      - xwalk-trace
      - xwalk-vision
      - xwalk-streaming
      - xwalk-quality
      - xwalk-deployment
      - developer-note
      - codescene-code-health
    steps:
      - name: Verify all xWalk module results
        env:
          JOB_RESULTS: ${{ toJSON(needs) }}
        run: |
          printf '%s\n' "$JOB_RESULTS" | jq .

          if ! printf '%s\n' "$JOB_RESULTS" |
              jq --exit-status 'all(.[]; .result == "success")' >/dev/null; then
              echo "::error::One or more xWalk module jobs did not succeed."
              exit 1
          fi

          echo "All xWalk module jobs completed successfully."
